'use client';

import Link from 'next/link';
import { useState } from 'react';
const plans = [
  {
    id: 'basico',
    name: 'BÃ¡sico',
    price: 'R$ 25',
    period: '/mÃªs',
    description: 'Ideal para freelancers iniciantes',
    features: [
      'AtÃ© 5 propostas por mÃªs',
      'Perfil bÃ¡sico',
      'Chat com clientes',
      'Suporte por e-mail',
      'Acesso a projetos pÃºblicos',
    ],
    notIncluded: [
      'Destaque no perfil',
      'Propostas ilimitadas',
      'RelatÃ³rios avanÃ§ados',
      'Suporte prioritÃ¡rio',
    ],
    popular: false,
    color: 'gray',
  },
  {
    id: 'profissional',
    name: 'Profissional',
    price: 'R$ 45',
    period: '/mÃªs',
    description: 'Para freelancers que querem crescer',
    features: [
      'Propostas ilimitadas',
      'Perfil verificado com selo',
      'Chat com clientes',
      'Suporte prioritÃ¡rio',
      'Acesso a todos os projetos',
      'Destaque nas buscas',
      'RelatÃ³rios de desempenho',
    ],
    notIncluded: [
      'Gerenciamento de equipe',
      'API de integraÃ§Ã£o',
    ],
    popular: true,
    color: 'green',
  },
  {
    id: 'empresa',
    name: 'Empresa',
    price: 'R$ 75',
    period: '/mÃªs',
    description: 'Para escritÃ³rios e equipes',
    features: [
      'Tudo do Profissional',
      'AtÃ© 5 membros na equipe',
      'Gerenciamento de projetos',
      'RelatÃ³rios avanÃ§ados',
      'API de integraÃ§Ã£o',
      'Suporte 24/7',
      'Gerente de conta dedicado',
      'Faturamento personalizado',
    ],
    notIncluded: [],
    popular: false,
    color: 'purple',
  },
];

const faqs = [
  {
    question: 'Posso cancelar a qualquer momento?',
    answer: 'Sim! VocÃª pode cancelar sua assinatura a qualquer momento. O acesso continua atÃ© o fim do perÃ­odo pago.',
  },
  {
    question: 'Como funciona o perÃ­odo de teste?',
    answer: 'Oferecemos 7 dias de teste grÃ¡tis do plano Profissional para novos usuÃ¡rios. NÃ£o Ã© necessÃ¡rio cartÃ£o de crÃ©dito.',
  },
  {
    question: 'Posso mudar de plano depois?',
    answer: 'Sim, vocÃª pode fazer upgrade ou downgrade do seu plano a qualquer momento. O valor serÃ¡ ajustado proporcionalmente.',
  },
  {
    question: 'Quais formas de pagamento sÃ£o aceitas?',
    answer: 'Aceitamos cartÃ£o de crÃ©dito, dÃ©bito, PIX e boleto bancÃ¡rio atravÃ©s do Mercado Pago.',
  },
  {
    question: 'O que acontece se eu exceder o limite de propostas?',
    answer: 'No plano BÃ¡sico, vocÃª precisarÃ¡ aguardar o prÃ³ximo mÃªs ou fazer upgrade para o plano Profissional.',
  },
];

export default function PlanosPage() {
  const [billingPeriod, setBillingPeriod] = useState<'mensal' | 'anual'>('mensal');
  const [openFaq, setOpenFaq] = useState<number | null>(null);

  const getPrice = (basePrice: string) => {
    const value = parseInt(basePrice.replace('R$ ', ''));
    if (billingPeriod === 'anual') {
      const annualValue = Math.round(value * 0.8); // 20% desconto
      return `R$ ${annualValue}`;
    }
    return basePrice;
  };

  return (
    <main className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100">
      {/* Navigation */}
      <nav className="bg-white shadow-sm px-6 py-4 flex justify-between items-center sticky top-0 z-50">
        <Link href="/" className="flex items-center gap-3">
          <div className="w-11 h-11 bg-gradient-to-br from-[#22C55E] to-[#16A34A] rounded-xl flex items-center justify-center text-white font-black text-xl shadow-lg">
            AF
          </div>
          <span className="text-2xl font-bold text-gray-800">ArquiFreelas</span>
        </Link>
        <div className="flex gap-4">
          <Link href="/login" className="px-5 py-2.5 border-2 border-[#22C55E] text-[#22C55E] rounded-xl hover:bg-[#22C55E] hover:text-white transition font-medium">
            Login
          </Link>
          <Link href="/cadastro" className="px-5 py-2.5 bg-gradient-to-r from-[#22C55E] to-[#16A34A] text-white rounded-xl hover:shadow-lg transition font-medium">
            Cadastro
          </Link>
        </div>
      </nav>

      {/* Hero */}
      <section className="bg-gradient-to-br from-[#22C55E] to-[#16A34A] text-white py-20 px-6">
        <div className="max-w-4xl mx-auto text-center">
          <h1 className="text-4xl md:text-5xl font-bold mb-4">
            Escolha o plano ideal para vocÃª
          </h1>
          <p className="text-xl opacity-90 mb-8">
            Comece grÃ¡tis por 7 dias. Cancele quando quiser.
          </p>
          
          {/* Billing Toggle */}
          <div className="inline-flex items-center bg-white/20 rounded-xl p-1.5">
            <button
              onClick={() => setBillingPeriod('mensal')}
              className={`px-6 py-2.5 rounded-lg font-medium transition ${
                billingPeriod === 'mensal' 
                  ? 'bg-white text-[#22C55E]' 
                  : 'text-white hover:bg-white/10'
              }`}
            >
              Mensal
            </button>
            <button
              onClick={() => setBillingPeriod('anual')}
              className={`px-6 py-2.5 rounded-lg font-medium transition flex items-center gap-2 ${
                billingPeriod === 'anual' 
                  ? 'bg-white text-[#22C55E]' 
                  : 'text-white hover:bg-white/10'
              }`}
            >
              Anual
              <span className="bg-yellow-400 text-yellow-900 text-xs px-2 py-0.5 rounded-full font-bold">
                -20%
              </span>
            </button>
          </div>
        </div>
      </section>

      {/* Plans Grid */}
      <section className="py-16 px-6 -mt-8">
        <div className="max-w-6xl mx-auto grid md:grid-cols-3 gap-8">
          {plans.map((plan) => (
            <div
              key={plan.id}
              className={`bg-white rounded-3xl shadow-xl overflow-hidden border-2 transition hover:shadow-2xl hover:-translate-y-2 ${
                plan.popular ? 'border-[#22C55E] relative' : 'border-gray-100'
              }`}
            >
              {plan.popular && (
                <div className="bg-gradient-to-r from-[#22C55E] to-[#16A34A] text-white text-center py-2 text-sm font-bold">
                  â­ MAIS POPULAR
                </div>
              )}
              
              <div className="p-8">
                <h3 className="text-2xl font-bold text-gray-800 mb-2">{plan.name}</h3>
                <p className="text-gray-500 mb-6">{plan.description}</p>
                
                <div className="flex items-baseline gap-1 mb-6">
                  <span className="text-5xl font-bold text-gray-800">{getPrice(plan.price)}</span>
                  <span className="text-gray-500">{plan.period}</span>
                </div>
                
                {billingPeriod === 'anual' && (
                  <p className="text-sm text-[#22C55E] mb-6">
                    Economia de R$ {parseInt(plan.price.replace('R$ ', '')) * 12 * 0.2}/ano
                  </p>
                )}

                <Link
                  href="/cadastro"
                  className={`w-full block text-center py-4 rounded-xl font-bold transition mb-8 ${
                    plan.popular
                      ? 'bg-gradient-to-r from-[#22C55E] to-[#16A34A] text-white hover:shadow-lg'
                      : 'bg-gray-100 text-gray-800 hover:bg-gray-200'
                  }`}
                >
                  ComeÃ§ar Agora
                </Link>

                <div className="space-y-4">
                  {plan.features.map((feature, index) => (
                    <div key={index} className="flex items-start gap-3">
                      <div className="w-5 h-5 bg-[#22C55E]/10 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                        <svg className="w-3 h-3 text-[#22C55E]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={3} d="M5 13l4 4L19 7" />
                        </svg>
                      </div>
                      <span className="text-gray-600">{feature}</span>
                    </div>
                  ))}
                  
                  {plan.notIncluded.map((feature, index) => (
                    <div key={index} className="flex items-start gap-3 opacity-50">
                      <div className="w-5 h-5 bg-gray-100 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                        <svg className="w-3 h-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={3} d="M6 18L18 6M6 6l12 12" />
                        </svg>
                      </div>
                      <span className="text-gray-400">{feature}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* Features Comparison */}
      <section className="py-16 px-6 bg-white">
        <div className="max-w-4xl mx-auto">
          <h2 className="text-3xl font-bold text-center text-gray-800 mb-4">
            Compare os planos
          </h2>
          <p className="text-center text-gray-500 mb-12">
            Veja em detalhes o que cada plano oferece
          </p>
          
          <div className="overflow-x-auto">
            <table className="w-full">
              <thead>
                <tr className="border-b-2 border-gray-100">
                  <th className="text-left py-4 px-4 text-gray-600 font-medium">Recurso</th>
                  <th className="text-center py-4 px-4 text-gray-800 font-bold">BÃ¡sico</th>
                  <th className="text-center py-4 px-4 text-[#22C55E] font-bold">Profissional</th>
                  <th className="text-center py-4 px-4 text-gray-800 font-bold">Empresa</th>
                </tr>
              </thead>
              <tbody>
                {[
                  { feature: 'Propostas por mÃªs', basico: '5', profissional: 'Ilimitado', empresa: 'Ilimitado' },
                  { feature: 'Perfil verificado', basico: false, profissional: true, empresa: true },
                  { feature: 'Destaque nas buscas', basico: false, profissional: true, empresa: true },
                  { feature: 'Chat com clientes', basico: true, profissional: true, empresa: true },
                  { feature: 'RelatÃ³rios', basico: 'BÃ¡sico', profissional: 'AvanÃ§ado', empresa: 'Completo' },
                  { feature: 'Membros da equipe', basico: '1', profissional: '1', empresa: '5' },
                  { feature: 'Suporte', basico: 'E-mail', profissional: 'PrioritÃ¡rio', empresa: '24/7' },
                  { feature: 'API de integraÃ§Ã£o', basico: false, profissional: false, empresa: true },
                ].map((row, index) => (
                  <tr key={index} className="border-b border-gray-100 hover:bg-gray-50">
                    <td className="py-4 px-4 text-gray-600">{row.feature}</td>
                    <td className="text-center py-4 px-4">
                      {typeof row.basico === 'boolean' ? (
                        row.basico ? (
                          <span className="text-[#22C55E]">âœ“</span>
                        ) : (
                          <span className="text-gray-300">âœ—</span>
                        )
                      ) : (
                        <span className="text-gray-600">{row.basico}</span>
                      )}
                    </td>
                    <td className="text-center py-4 px-4 bg-[#22C55E]/5">
                      {typeof row.profissional === 'boolean' ? (
                        row.profissional ? (
                          <span className="text-[#22C55E]">âœ“</span>
                        ) : (
                          <span className="text-gray-300">âœ—</span>
                        )
                      ) : (
                        <span className="text-[#22C55E] font-medium">{row.profissional}</span>
                      )}
                    </td>
                    <td className="text-center py-4 px-4">
                      {typeof row.empresa === 'boolean' ? (
                        row.empresa ? (
                          <span className="text-[#22C55E]">âœ“</span>
                        ) : (
                          <span className="text-gray-300">âœ—</span>
                        )
                      ) : (
                        <span className="text-gray-600">{row.empresa}</span>
                      )}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </section>

      {/* FAQ Section */}
      <section className="py-16 px-6">
        <div className="max-w-3xl mx-auto">
          <h2 className="text-3xl font-bold text-center text-gray-800 mb-4">
            Perguntas Frequentes
          </h2>
          <p className="text-center text-gray-500 mb-12">
            Tire suas dÃºvidas sobre nossos planos
          </p>
          
          <div className="space-y-4">
            {faqs.map((faq, index) => (
              <div
                key={index}
                className="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden"
              >
                <button
                  onClick={() => setOpenFaq(openFaq === index ? null : index)}
                  className="w-full text-left px-6 py-5 flex items-center justify-between hover:bg-gray-50 transition"
                >
                  <span className="font-semibold text-gray-800">{faq.question}</span>
                  <svg
                    className={`w-5 h-5 text-gray-500 transition-transform ${openFaq === index ? 'rotate-180' : ''}`}
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
                  </svg>
                </button>
                {openFaq === index && (
                  <div className="px-6 pb-5 text-gray-600">
                    {faq.answer}
                  </div>
                )}
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 px-6 bg-gradient-to-br from-[#22C55E] to-[#16A34A] text-white">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-4xl font-bold mb-4">Pronto para comeÃ§ar?</h2>
          <p className="text-xl opacity-90 mb-8">
            Junte-se a milhares de profissionais que jÃ¡ estÃ£o usando ArquiFreelas
          </p>
          <div className="flex gap-4 justify-center flex-wrap">
            <Link href="/cadastro" className="px-8 py-4 bg-white text-[#22C55E] rounded-xl font-bold hover:bg-gray-100 transition shadow-lg">
              ComeÃ§ar Teste GrÃ¡tis
            </Link>
            <Link href="/projetos" className="px-8 py-4 border-2 border-white text-white rounded-xl font-semibold hover:bg-white hover:text-[#22C55E] transition">
              Ver Projetos
            </Link>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-gray-900 text-white py-16 px-6">
        <div className="max-w-6xl mx-auto grid md:grid-cols-4 gap-8">
          <div>
            <div className="flex items-center gap-3 mb-4">
              <div className="w-11 h-11 bg-gradient-to-br from-[#22C55E] to-[#16A34A] rounded-xl flex items-center justify-center text-white font-black text-xl">
                AF
              </div>
              <span className="text-xl font-bold">ArquiFreelas</span>
            </div>
            <p className="text-gray-400 text-sm">A plataforma que conecta arquitetos e freelancers de todo o Brasil.</p>
          </div>
          <div>
            <h4 className="text-[#22C55E] font-semibold mb-4">Empresa</h4>
            <ul className="space-y-2 text-gray-400">
              <li><Link href="#" className="hover:text-[#22C55E] transition">Sobre NÃ³s</Link></li>
              <li><Link href="#" className="hover:text-[#22C55E] transition">Blog</Link></li>
              <li><Link href="#" className="hover:text-[#22C55E] transition">Carreiras</Link></li>
            </ul>
          </div>
          <div>
            <h4 className="text-[#22C55E] font-semibold mb-4">Suporte</h4>
            <ul className="space-y-2 text-gray-400">
              <li><Link href="#" className="hover:text-[#22C55E] transition">Central de Ajuda</Link></li>
              <li><Link href="#" className="hover:text-[#22C55E] transition">Contato</Link></li>
              <li><Link href="#" className="hover:text-[#22C55E] transition">FAQ</Link></li>
            </ul>
          </div>
          <div>
            <h4 className="text-[#22C55E] font-semibold mb-4">Legal</h4>
            <ul className="space-y-2 text-gray-400">
              <li><Link href="#" className="hover:text-[#22C55E] transition">Termos de ServiÃ§o</Link></li>
              <li><Link href="#" className="hover:text-[#22C55E] transition">Privacidade</Link></li>
              <li><Link href="#" className="hover:text-[#22C55E] transition">Cookies</Link></li>
            </ul>
          </div>
        </div>
        <div className="border-t border-gray-800 mt-12 pt-8 text-center text-gray-500">
          <p>&copy; 2025 ArquiFreelas. Todos os direitos reservados.</p>
        </div>
      </footer>
    </main>
  );
}

